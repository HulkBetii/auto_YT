name: DR Pipeline — Process Jobs

on:
  schedule:
    - cron: "*/5 * * * *"   # every 5 minutes
  workflow_dispatch:          # allow manual trigger

jobs:
  process:
    runs-on: ubuntu-latest
    timeout-minutes: 6
    steps:
      - name: Trigger chain cycle
        run: |
          curl -sf \
            -H "Authorization: Bearer ${{ secrets.WEB3_DASHBOARD_SECRET }}" \
            "${{ vars.WEB3_BASE_URL }}/api/cron/process-jobs" \
            | jq .
